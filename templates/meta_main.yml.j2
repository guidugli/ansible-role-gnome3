---
# Generated file. Do not edit directly.
# Source template: {{ template_name }}
dependencies: []
galaxy_info:
  role_name: gnome3
  namespace: guidugli
  author: guidugli
  company: Creuzo Tech
  description: >-
    Configure GNOME 3+ desktop and GDM settings on Linux systems while keeping
    release metadata aligned with the shared Molecule platform matrix.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
{% for platform in platforms %}
    - name: {{ platform.name }}
      versions:
{%   for version in platform.versions %}
        - {{ version | tojson }}
{%   endfor %}
{% endfor %}
  galaxy_tags:
    - gnome3
    - gnome
    - desktop
    - gdm
    - dconf
    - linux
    - workstation
    - configuration
    - ui
